/**
 * @param {Number} ch
 * @param {Number} fat
 * @param {Number} protein
 * @constructor
 */
function Macros(ch, fat, protein) {
    this.ch = ch
    this.fat = fat
    this.protein = protein
}

module.exports = Macros
